{# Sert le formulaire d'ajout d'un produit #}
{% extends "base.html.twig" %}

{% block body %}
    {% if product is defined %}
        <h1>Formulaire de modification d'un produit</h1>
    {% else %}
        <h1>Formulaire de création d'un produit</h1>
    {% endif %}

    {{ form(form, {'attr':{novalidate:'novalidate'}}) }}

    {% if product is defined %}
        <a href="{{ path('manage_product_delete_confirm',{'id':product.id}) }}" class="Button -danger">Supprimer</a>
    {% endif %}

{% endblock body %}