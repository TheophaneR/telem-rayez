<ul>
    <li>Nom du produit : {{ product.name }}</li>

    {% set description = product.description %}

    {% if description is not empty %}
        <li>Description : {{ description }}</li>
    {% else %}
        <li>Il n'y a pas de desc</li>
    {% endif %}
    <li>Quantité en stock : {{ product.quantityInStock }}</li>
    <li>Prix : {{ product.price|price }}</li>
    <li>Ajouté au catalogue le : {{ product.createdAt|dateFr }}</li>
    <li>
        <img width="200px" src="/build/images/{{ product.imageName }}" alt="">
    </li>
    {% if products is not defined %}
    <li><a href="{{ path('product_show_all') }}">Voir tous les produits</a></li>
    {% else %}
    <li><a href="{{ path('product_show', {'id':product.id}) }}">Voir la fiche du produit</a></li>
    {% endif %}
</ul>